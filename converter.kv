BoxLayout:
    orientation: 'vertical'
    Label:
        text: 'Current trip location: ' + app.current_location(app.currentDate)
    Spinner:
        values: app.countries()
    TextInput:
        input_filter: 'float'
    Label:
        id: home
        text: app.home_country()
    TextInput:
    Label:
        text: app.currentDate
    Label:
    Button: